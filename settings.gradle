pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id "${jandexPluginId}" version "${jandexPluginVersion}"
        id "${grgitPluginId}" version "${grgitPluginVersion}"
    }
}

rootProject.name = 'doraemon'

include 'lang'
include 'panache'
include 'auditing'
include 'jpa'
include 'graphql'
include 'graphql-builder'
include 'basic-tree'
include 'mptt-tree'


