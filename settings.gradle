pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id "${jandexPluginId}" version "${jandexPluginVersion}"
        id "${grgitPluginId}" version "${grgitPluginVersion}"
    }
}

rootProject.name = 'doraemon'

include 'common-lang'
include 'common-panache'
include 'common-auditing'
include 'common-jpa'
include 'common-graphql'


