apply from: "${rootProject.rootDir}/gradle/hibernate.gradle"

dependencies {
    api "jakarta.persistence:jakarta.persistence-api:${jakartaPersistenceApiVersion}"
    api "io.quarkus:quarkus-security:${quarkusVersion}"
}
